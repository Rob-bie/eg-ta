import { BiomeType } from '../types/biome.d';

export const generationConfig = {
    tileWidth: 16,
    tileHeight: 16,
    mapBiome: BiomeType.Forest,
    mapWidth: 50,
    mapHeight: 50,
    mapScale: 1
};